# Silicon Radar — Python dependencies
# Install with: pip install -r requirements.txt

# Gemini AI (new SDK — replaces deprecated google-generativeai)
google-genai>=2.0.0

# Database
psycopg2-binary>=2.9.9

# HTTP + RSS
feedparser>=6.0.11
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Web search (source discovery)
ddgs>=9.0.0

# YouTube transcripts (no API key needed)
youtube-transcript-api>=1.0.0

# Telegram
python-telegram-bot>=21.0

# Supabase client (REST over HTTPS — bypasses psycopg2/pooler)
supabase>=2.0.0

# Twitter/X scraping
twscrape>=0.14.0

# Utilities
python-dotenv>=1.0.0
